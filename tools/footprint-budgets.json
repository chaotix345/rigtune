{
  "formatVersion": 1,
  "mode": "warn",
  "about": "RigTune's own footprint budgets (docs/v0.4/SPEC.md 10), read by FrameHookBudgetTest and FootprintGameTest. ceiling = the SPEC's initial ceiling (null where the SPEC sets none); limit = min(ceiling, 2 x the largest value observed in the calibration, docs/v0.4/verification/footprint/README.md). mode warn logs a value past its limit, fail fails the test.",
  "budgets": {
    "renderThreadInitWallMs": {"limit": 25, "ceiling": 25, "what": "wall time of RigTunePreLaunch + onInitializeClient on the render thread"},
    "renderThreadInitCpuMs": {"limit": 15, "ceiling": 15, "what": "render-thread CPU time of RigTunePreLaunch + onInitializeClient"},
    "clientStartedWallMs": {"limit": 100, "ceiling": null, "what": "wall time of RigTune's CLIENT_STARTED handler (RealController.start) on the render thread"},
    "workerCpuMs5s": {"limit": 300, "ceiling": 300, "what": "CPU of every RigTune thread in the 5 s after CLIENT_STARTED"},
    "frameHookNsPerCallOff": {"limit": 20, "ceiling": 20, "what": "per-frame hook, ns per call, session monitor off (10 M calls)"},
    "frameHookAllocBytesOff": {"limit": 0, "ceiling": 0, "what": "per-frame hook, bytes allocated, monitor off: the hot calls, or the first 10 M calls from cold beyond 4 KiB of JIT noise"},
    "frameHookNsPerCallOn": {"limit": 200, "ceiling": 200, "what": "per-frame hook, ns per call, session monitor on (after WS-S)"},
    "frameHookAllocBytesOn": {"limit": 0, "ceiling": 0, "what": "per-frame hook, bytes allocated as above, monitor on (after WS-S)"},
    "tickHookNsPerCall": {"limit": 2000, "ceiling": 2000, "what": "RigTune's END_CLIENT_TICK hook, ns per call on the render thread"},
    "tickHookAllocBytes": {"limit": 0, "ceiling": 0, "what": "RigTune's END_CLIENT_TICK hook, bytes allocated per 100,000 calls after warm-up"},
    "rigtuneClassBytesIdle": {"limit": 8388608, "ceiling": 8388608, "what": "bytes of RigTune-class instances after a full GC at idle (class histogram, shallow)"},
    "heapGrowthAfterCyclesBytes": {"limit": 8388608, "ceiling": null, "what": "heap used after a full GC: after 20 RigTune screen + Tools open/close cycles minus before them"},
    "leakSuspects": {"limit": 0, "ceiling": 0, "what": "RigTune classes with at least one more live instance per open/close cycle"},
    "monitorOnRetainedBytes": {"limit": 2621440, "ceiling": 2621440, "what": "session monitor on: StutterMonitor.retainedBytes() (after WS-S)"},
    "monitorOffRetainedBytes": {"limit": 262144, "ceiling": 262144, "what": "session monitor off again: StutterMonitor.retainedBytes() (after WS-S)"},
    "samplerCpuMsPer60s": {"limit": 30, "ceiling": 30, "what": "\"RigTune stutter sampler\" CPU per 60 s with the monitor on (after WS-S)"}
  }
}
