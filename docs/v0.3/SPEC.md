# RigTune v0.3.0: specification

Status: DRAFT (Phase 2). Each item has acceptance criteria (AC). An item ships only when every AC is verified, and the evidence is recorded in docs/PROGRESS.md. Priorities: P0 must ship; P1 should ship (defer with a written reason); P2 stretch. Cut P2 before compromising P0/P1 quality.

Research inputs: docs/research/v0.3/{mc-versions,ci-gametests,launcher-ram,hardware-tiers,benchmark,misc}.md. Orientation (2026-09-26): the user's real instance still runs 0.1.0 (last played 2026-09-25 09:08; no benchmark, no history); the Modrinth project is still in review; no update-rules PR is open.

Compatibility promise:
- A 0.1.x client never gets an unsafe or less conservative recommendation from `rules-v1.json` (RulesV1DifferentialTest + check_rules_v1.py).
- A 0.2.x client never gets an unsafe recommendation from `rules-v2.json`: a new condition field fails closed; a new rule-level field is either ignored-safe for 0.2.0 (it can only take a recommendation away) or the rule carries a `requires` feature 0.2.0 doesn't know; a new tier-table column needs a new schema file.
- Every file 0.1.0 and 0.2.0 wrote (`pending.json`, `last-apply.json`, `history.json`, `benchmarks.json`, `settings.json`, `rigtune.json`, `rules-cache.json`, `rules-v2-cache.json`, `benchmark-restore.json`, `helper/`) keeps working after an upgrade to 0.3.0. 0.3.0 doesn't change any of their formats (no `formatVersion`/`schemaVersion` bump), so a downgrade to 0.2.0 keeps working too; any new optional field must be one 0.2.0 ignores on read.

---

## 1. Newer Minecraft versions (P0)
Sources: docs/research/v0.3/mc-versions.md.
- Verified 2026-09-26 (Mojang manifest, Fabric meta, Modrinth): the newest stable release is 26.3 (2026-09-15); no 26.2.x/26.3.x hotfix exists; 26.4 is `26.4-snapshot-1` only (2026-09-22; release estimated mid-December from past cadence, not announced). **v0.3.0 ships 26.2 + 26.3 only.**
- The trial on 26.4-snapshot-1 (throwaway worktree) compiled with no code change and passed 848/848 unit tests; the class files are byte-identical to 26.3's, and a javap diff of all 308 MC/Fabric/Mod Menu member refs found nothing that breaks. What changes is behaviour: 26.4 makes Vulkan the default renderer (the `vulkan-backend` advice would be wrong), and on a snapshot Loader's normalized version (`26.4-alpha.1`) isn't a Modrinth game version.
- `minecraft` ranges stay `~26.2` / `~26.3` (checked with Loader 0.19.5's own classes: `~26.3` accepts 26.3.1, 26.3.2 and 26.3.1-rc-1 and rejects every 26.4 build). Never an open-ended `>=`.
- One-time changes so that adding a version is one command:
  - A. Sodium on `localRuntime` only when `sodium_version` is set (a node without a Sodium build can still run its unit tests).
  - B. `release.yml` publishes every `versions/*/` node in a loop (each failure recorded, the others still run); `versionType` is `alpha` for a node whose MC id contains `-snapshot-`/`-pre-`/`-rc-`.
  - C. `tools/update_rules.py` takes its MC targets from the Stonecutter version list plus each node's Modrinth hotfix tags (`<node>.x`), not "the newest 3 releases" (which would drop 26.2 after a 26.4.x hotfix); `--mc-versions` stays an override.
  - D. `tools/mc_apidiff.py <prev> <mc>`: the javap API-diff harness from the trial (every MC/Fabric/Mod Menu member RigTune's sources reference, resolved against both versions' jars).
  - `tools/add_mc_version.py <mc> [--prerelease-ok] [--dry-run]`: checks the Mojang manifest (release unless `--prerelease-ok`, Java 25), Fabric meta and the Fabric API maven; fetches Mod Menu/Sodium/Iris from Modrinth; inserts the version into the Stonecutter list and writes `versions/<mc>/gradle.properties`; prints the human checklist (build, `//? if` fixes, mc_apidiff, game tests + production smoke, rules review, docs, release, hotfix bytecode check).
  - Modrinth queries use `FabricLoader.getRawGameVersion()` (normalized version kept for `mcVersionRange`), so a snapshot jar would still resolve mods.
  - Rules: `vulkan-backend` gets `"mcVersionRange": "<26.4-"` (item 7's rules run).
  - DESIGN.md "Porting" is replaced by the checklist (research §4.4).
- AC1.1 PROGRESS records the verified version state and date. AC1.2 Python unit tests (offline fixtures of the Mojang manifest, Fabric meta, maven-metadata and Modrinth responses) cover add_mc_version.py: a release adds the node and writes the properties; a snapshot without `--prerelease-ok` is refused; no Sodium build → no `sodium_version`; an existing node is refused; `--dry-run` writes nothing. AC1.3 `python tools/add_mc_version.py 26.4-snapshot-1 --prerelease-ok --dry-run` against the live APIs prints the expected edits (run once, output saved to docs/v0.3/verification/). AC1.4 update_rules.py's targets come from the version list (unit test with a fake tag list including `26.4.1`). AC1.5 The release workflow loop and `versionType` are covered by a dry-run of `:26.2:modrinth` configuration (Minotaur debug mode) or a workflow lint, and the real v0.3.0 release publishes both nodes. AC1.6 mc_apidiff.py reproduces "no breaking change 26.3 → 26.4-snapshot-1" (output saved).

## 2. Client game tests in CI (P0)
Sources: docs/research/v0.3/ci-gametests.md (prototype branch `research/ci-gametest`: 23/23 legs green over 7 runs, 0 flakes; about 4 minutes per leg; a deliberate failure turned the job red with artifacts, run 36163460374).
- A `client-gametest` job in build.yml runs all client game-test classes with Loom's `ClientProductionRunTask` (`runProductionClientGameTest`: the jar that ships, on the production classpath; Fabric's recommendation for CI) on **ubuntu-24.04** (pinned: ubuntu-latest moves to 26.04 in November 2026). Loom 1.17 wraps the run in xvfb-run itself when `CI` is set. The dev `runClientGameTest` stays for local work.
- Legs: every `versions/*/` node with OpenGL (Mesa llvmpipe, GL 4.5), plus 26.3+ with Vulkan (lavapipe). The matrix is generated from `versions/*/` by a small setup step, so a new node gets its legs without editing the workflow. A step fails the leg unless `latest.log` names the backend the leg is meant to test.
- 26.3 (SDL3) asks for an sRGB GLX visual Xvfb doesn't have; `SDL_OPENGL_FORCE_SRGB_FRAMEBUFFER=skip` fixes it (without it OpenGL fails and, with no Vulkan driver, the game hangs on a blocking dialog). Sound fails to open and the game carries on muted.
- Artifacts on success and failure: screenshots, `logs/`, crash reports, per leg. A 20-minute step timeout bounds hangs; no automatic retries (a flake is re-run by hand, so real regressions aren't hidden).
- AC2.1 The job is green on every leg on the v0.3.0 PR (and on every Wave A merge). AC2.2 A deliberately failing game test turns the job red and the artifacts still upload (proven on the prototype; re-checked once on the real job before WS-0 merges, then reverted). AC2.3 The coordinator reviews screenshots from each leg (not black; the RigTune screens render) on the foundation merge and on the release candidate. AC2.4 `runClientGameTest` still works locally on 26.2 and 26.3 (Phase 5). AC2.5 The matrix script lists every node in `versions/*/` (checked with a temporary extra node in a dry run or a unit test of the script).

## 3. Deferred v0.2 defects (P0)
Sources: docs/reviews/review-4.md ("Deferred to v0.3.0"), orientation of the real instance.

### 3a. `Staging.dropQueuedUpdates` scope and `staged` bookkeeping
- Today (client/undo/Staging.java:195) every staged ENABLE_FILE whose mod id has a jar in `mods/update/` is dropped with its group on every rebuild, even when that mod isn't loaded. A stale jar there cancels an addition or an undo re-enable forever; and RealController's `staged` set (RealController.java:88/308) only clears `update:<id>`.
- Fix: drop a staged group only when it is RigTune's **update** of a **loaded** mod whose own update is queued: the group has an ENABLE_FILE with that mod id AND the mod id is loaded (ModScanner's loaded ids) AND the mod has a queued jar in `mods/update/`. Additions and undo re-enables of a mod that isn't loaded are kept. After a drop, the client's staged bookkeeping is recomputed from `pending.json` (every recommendation id whose ops left pending is cleared, not only `update:<id>`), so the report ticks/labels match what's really staged.
- AC3.1 Unit tests: an addition of an unloaded mod with a stale queued jar stays staged; an undo re-enable of an unloaded mod stays staged; an update of a loaded mod with a queued update is dropped (with the notice); the staged ids after a drop equal the ids still in pending.json.

### 3b. DependencyResolver judges the post-update state
- Today (core/modrinth/DependencyResolver.java:88-114) a version-specific Modrinth `incompatible` dependency is judged against the pre-update installed version ids, and the batch's own updates aren't in the "going in together" set.
- Fix: the resolver's view is "installed, with every update in this batch applied": an installed project with a batched update counts with the update's version id (the old one no longer counts), and every batched update version joins the set checked against `incompatible` entries (both directions, as for additions).
- AC3.2 Unit tests: (i) B declares `incompatible` on A's version v1; A is installed at v1 and updated to v2 in the same batch → B is not refused; (ii) B declares `incompatible` on A v2, which is being updated to in the batch → refused, naming A; (iii) an update's own version declaring `incompatible` on an installed project → refused; (iv) the existing tests stay green.

### 3c. No raw version ids in messages
- A version-only dependency's message shows a Modrinth version id today (DependencyResolver.java:101-103). Fix: name the project the version belongs to (its title, from the version lookup; slug, then "another mod" as fallbacks), never a raw id.
- AC3.3 A unit test asserts the message for a version-only incompatible dependency contains the project title and matches no base62 version-id pattern of the fixture.

### 3d. Weekly rules PRs
- None open on 2026-09-26. Before the release, any open `bot/rules-update-*` PR is triaged: merged into feat/v0.3.0 (after a green CI and a read of its REVIEW.md) or closed with a written reason.
- AC3.4 PROGRESS.md records every rules PR seen during v0.3 and its outcome.

### 3e. Real-instance feedback
- The only real-world failure (0.1.0's DH update racing DH's own updater) is fixed in 0.2; the queued DH build in the user's `mods/update/` is a multi-loader jar whose `fabric.mod.json` id is `distanthorizons`, so 0.2+ detects it (checked read-only 2026-09-26).
- The failure's reason was only in `config/rigtune/helper.log`. Fix (with item 6): at startup, when `last-apply.json` has failed ops, RigTune logs one WARN line per failed op in `latest.log` (op, mod or file name, reason, attempt n of 3) and item 6's history screen shows the reason next to the change.
- AC3.5 A unit test over a real 0.1.0-shaped failed `last-apply.json` (the captured fixture) produces the WARN lines; the history screen shows "Last attempt failed: ..." for that change (item 6 game test).

### 3f. Benchmark render-distance steps load no extra terrain (found in v0.3 research)
- ClientKnobs applies render distance in memory (`SettingsBridge.applyVanilla(..., save=false)`), and only `Options.save()` calls `broadcastOptions()`, so the (integrated) server keeps the join-time view distance: after RD 5 → 12 in memory the client held 162/167 chunks (26.2/26.3) for 10 s; within about 1 s of `options.broadcastOptions()` it held 637 on both (benchmark.md §c). Tune's up-steps therefore measure the start-RD chunk set and can recommend too high a render distance.
- Fix: after every render-distance or simulation-distance change the benchmark makes (each step and the restore), call `options.broadcastOptions()` (no options.txt write), and wait until the chunk count settles before measuring.
- AC3.6 = AC8.1-AC8.3, plus a game test in the benchmark world that joins at RD 5, runs a step at RD 12 and asserts every chunk within 11 of the camera is loaded before that step records its first frame (26.2 and 26.3). AC3.7 A real `runBenchmarkAutorun` Tune on 26.2 and 26.3 (Phase 5) records per-step chunk counts that grow with RD.

## 4. Self-update, moderation and publishing (P0)
- Modrinth: status `processing` on 2026-09-26. If it's approved during v0.3, check the real instance's logs (read-only) for the offered update once the user has played; otherwise record that the in-the-wild check waits on moderation. Moderator messages can't be read with the token (no thread scopes): ask the user to forward any.
- The self-update E2E harness (tools/e2e) runs **0.2.0 → 0.3.0** (the released 0.2.0 jar, its sha256 recorded) and **0.1.0 → 0.3.0** (the user's own instance is on 0.1.0, so this is the path real users take), plus the undo-after-restart scenario on 0.3.0.
- Release: GitHub release with one jar + sources jar per MC version; Modrinth versions `0.3.0+mc<mc>` byte-identical (the release job's SHA-512 check through the authenticated version list); `tools/modrinth_project.py submit` then `sync-body` (body docs/modrinth/body-0.3.md).
- AC4.1 Both E2E runs PASS with evidence under docs/smoke/self-update/final-v020-to-030/ and final-v010-to-030/. AC4.2 undo-after-restart PASS on the 0.3.0 candidate (docs/smoke/self-update/undo-after-restart-030/). AC4.3 The release job is green, including "Verify Modrinth files match". AC4.4 `modrinth_project.py status` after `submit` shows the 0.3.0 versions listed with an environment set. AC4.5 raw rules-v1.json and rules-v2.json on main return HTTP 200 and parse.

## 5. Launcher-aware RAM advice (P1)
Sources: docs/research/v0.3/launcher-ram.md.
- The rules keep deciding **when** and **how much** (the `ram-*` advice rules; the research found their thresholds corroborated: 2 GB launcher defaults are too little for modded play, 6 GB for Distant Horizons or shaders, about 8 GB with both, no more than about half of system RAM on small machines, a ceiling around 8 GB because a larger G1 heap lengthens pauses). 0.3 adds **how**: the launcher's exact steps.
- Detection (pure core, `core/launcher/`), from signals the running JVM can read, in this order; every signal is optional and a missing one is just "not this launcher":
  1. `org.prismlauncher.instance.name` or `multimc.instance.title` system property, or the `INST_ID`/`INST_NAME` environment variables → **Prism Launcher** (MultiMC family).
  2. `minecraft.launcher.brand` = `theseus` → **Modrinth App**; = `ATLauncher` → **ATLauncher**; the official launcher's literal only if verified from primary evidence during implementation (else those users get the generic text).
  3. Walking up from the game dir (at most 3 levels): `instance.cfg` + `mmc-pack.json` → Prism (corroborates 1; `MaxMemAlloc` read); `minecraftinstance.json` → **CurseForge app** (reads `isMemoryOverride`, `allocatedMemory`).
  4. Otherwise **Unknown**: today's generic wording, no launcher named.
  Path substrings are never the only signal. Only these property/env names are read; nothing about the launcher leaves the machine except the launcher *name* in the share report the user copies.
- UI: every advice whose id starts with `ram-` (documented in RULES_SCHEMA.md as the convention) gets one extra line: "In <Launcher>: <click-steps>" (translatable, one key per launcher; CurseForge uses the per-pack "Profile Options" steps when that pack overrides memory, else the global Java settings). The hardware header's memory line names the launcher ("6 GB of 32 GB, set in the Modrinth App"). Click-step wording comes from the launcher's own UI strings where the launcher is open source (Modrinth App, Prism, ATLauncher: cite file and line in the research doc) and from the best available sources otherwise (marked).
- AC5.1 Unit tests for detection: each launcher from its real signals (property/env/file fixtures copied from real formats), precedence (Prism property beats a `theseus` brand), malformed/oversized `instance.cfg`/`minecraftinstance.json` → no crash, no value, Unknown only when nothing matches, no signal → Unknown. AC5.2 Scenario tests over the bundled rules: the `ram-*` advice for (8 GB system, 2 GB heap), (16 GB, 2 GB, DH), (32 GB, 6 GB, DH + shaders), (32 GB, 16 GB) gets the right launcher line for each launcher and none for Unknown. AC5.3 UiGameTest screenshots at 3 sizes show the launcher line with `-Dminecraft.launcher.brand=theseus` and without it. AC5.4 The share report names the launcher and nothing else about it (unit test).

## 6. "What RigTune changed" screen with per-entry undo (P1)
- A new **History** screen, opened from the RigTune screen, lists `history.json` entries newest first: kind (Apply, Benchmark keep, Undo, Imported from 0.1), local date and time, RigTune/MC version, and a one-line summary ("3 settings, 2 mods"). Selecting an entry shows its changes: settings with their label (settingLabels) and before → after; mod files as "Added/Disabled/Re-enabled <mod>"; each with a status label: Applied, Waiting for restart (STAGED), Not applied (ABANDONED), Cancelled (DISCARDED), Undone (REVERTED). A STAGED change whose op failed at the last exit shows the helper's reason from `last-apply.json` and the attempt count (item 3e).
- Per-entry undo: **Undo this** on an apply or benchmark entry that isn't fully undone → `UndoPlanner.planEntry(entries, pending, state, entryId)` → the existing UndoScreen confirmation (REVERT / DISCARD_STAGED / SKIP with reasons) → `undo(plan)`, which re-checks each item exactly as today (M8). A change that a later entry changed again is skipped ("changed again by a later apply"). Undo entries aren't undoable (redo stays a non-goal). "Undo last" and "Undo everything" stay as they are.
- Empty, corrupt (`.bad` backup made) and newer-format history each show a clear read-only message. No history.json format change.
- AC6.1 UndoPlanner unit tests for planEntry: newest entry equals today's "undo last"; an older entry whose settings were changed again later → SKIP; an older entry's mod addition still present → REVERT; an already-undone entry → nothing to do; staged-only entry → DISCARD_STAGED; unknown id → empty plan. AC6.2 A core model test maps every status and kind to its label key and summarises entries (counts). AC6.3 A game test opens History with a seeded history.json (apply + benchmark + undo + a failed STAGED change), screenshots at 3 sizes, runs Undo this on the older apply and checks the values and the new undo entry. AC6.4 Localised (no hard-coded strings; item 9's check passes).

## 7. Hardware table refresh (P1)
Sources: docs/research/v0.3/hardware-tiers.md.
- The existing patterns already cover RTX 50 desktop/laptop, RX 9070 XT/9070/9060 (XT), Arc B580/B570, Radeon 890M/880M/8060S, Arc 140V/B370/B390, Apple M-series and Ryzen 9000/X3D, Core Ultra 200S K. Changes (inserted before the rows noted in the research):
  - `RX 9070 GRE` → tier 4 (was wrongly tier 5 via the generic 9070 row). More conservative, so also in v1.
  - `Arc Pro B50` → tier 3, `Arc Pro B6x/B7x` → tier 4, `integrated: false`. (Correction found by WS-D's tests: they didn't hit the integrated catch-all as the research thought; they got the Intel vendor fallback, tier 2, discrete.) This raises them, so these rows carry `"v1": false`: 0.1.x keeps today's classification.
  - `RTX 5050` (not `5050 Ti`) → tier 3 (same as today's vendor fallback; explicit now). Also in v1.
- Tooling: `tools/update_rules.py` and `tools/check_rules_v1.py` accept the maintainer-only `"v1": false` on `gpuTiers`/`cpuTiers` rows (omitted from rules-v1.json, the key never written to either output); REVIEW.md section (d) lists them; RULES_SCHEMA.md documents it.
- No new CPU rows (every current family matches an existing generation-agnostic pattern or lands sensibly on the formula without a verified OSHI string).
- AC7.1 GpuClassifierTest/CpuClassifierTest gain every case in research §4, including the negative ones. AC7.2 RulesV1DifferentialTest's hardware matrix gains an RTX 5070, RX 9070 GRE, Arc Pro B60 and Ryzen 7 9800X3D; it passes against the regenerated rules-v1.json. AC7.3 Python tests cover the tier `v1: false` projection and check_rules_v1.py. AC7.4 Rules regenerated (one revision for both files), `./gradlew build` rerun and green; bundled copy identical.

## 8. Benchmark follow-up (P1)
Sources: docs/research/v0.3/benchmark.md (terrain survey of 9,409 samples per version and 16 screenshots in the research scratchpad).
- The user has no benchmark history, so the choice is on product value. Built: the render-distance fix (3f, a Tune correctness bug), (c) a scene-variety guard, (a) a read-only shader advice line, and a `context` record that makes future comparisons possible. **Deferred to v0.4: (b)'s median comparison UI** (zero value until at least 3 comparable runs exist, and today nothing records the resolution, DH or shader state, so comparable runs can't be identified; the `context` field fixes that from 0.3.0 on).
- (c) Evidence: the camera sees a forested hilltop at (0, 192) on both versions (terrain height and biome identical at all 9,409 samples within ±768 blocks; only tree placement differs; an ocean and beach 130-200 blocks east at RD 12). The view is fine; no seed or location change. v0.2's note that the versions "generate different terrain" is corrected in DESIGN.md.
  - `core/benchmark/SceneVariety.check(grid)` (pure): rejects "ocean" (> 50% water within 128 blocks), "flat" (height sd < 8 or range < 24 within 128) and "monotone" (fewer than 2 biomes at ≥ 5% within 192).
  - BenchmarkGameTest samples the benchmark world's generator heights/biomes around the camera on the server thread and fails when the check rejects it, on every supported MC version (so a new MC version's worldgen change is caught), logging a scene fingerprint.
  - Camera height becomes the terrain floor + 16 (133 on both versions) instead of depending on a tree.
- (a) Shader advice (no profile switching: Iris' API has no option/profile access and profiles are per pack): the result screen shows one advice line exactly when the shader cost was measured, the 1% low with shaders is below the target, the 1% low without shaders reaches it, and the gap is at least 10%: "Your shader pack costs about N%. A lighter profile in the pack's settings (Options > Video Settings > Shader Packs > Shader Pack Settings) may reach your target." Nothing is written to `iris.properties` or `shaderpacks/*.txt`.
- `context` (optional object on each new benchmarks.json run; `schemaVersion` stays 1): DH rendering on/off, shaders on/off, shader pack name, framebuffer width and height, fullscreen, benchmark protocol version.
- AC8.1 (3f) ClientKnobs broadcasts after every RD/SD change and on every restore path, never writing options.txt (unit test with a counting fake). AC8.2 A step's settle phase ends only when every chunk within RD−1 of the camera is present on the client and its sections are built, or at a 20 s timeout that logs the missing count (unit test on the settle logic + the 3f game test). AC8.3 After a finished or cancelled Tune, the integrated server's requested view distance equals the restored render distance (game test). AC8.4 SceneVariety unit tests: all-ocean → "ocean", flat → "flat", single biome → "monotone", the recorded camera grid → accepted. AC8.5 BenchmarkGameTest runs the variety check and logs the fingerprint on 26.2 and 26.3 (identical metrics). AC8.6 Camera y = floor + 16 on both versions (game test log). AC8.7 Shader advice truth table (unit test) and a production smoke with a shader pack showing it leaves every VALUE in `iris.properties` and `shaderpacks/*.txt` unchanged (amended in Phase 5: Iris itself rewrites both files, with a new `#date` first line, whenever shaders are toggled through its API and at every game start; RigTune writes neither). AC8.8 `context` is written for new runs; a 0.2.0-written benchmarks.json fixture loads unchanged; the pinned 0.2.0 reader (a copy of 0.2.0's BenchmarkHistory/BenchmarkRecord in the test tree) loads a 0.3.0-written file.

## 9. Localisation (P1)
Sources: docs/research/v0.3/misc.md §D.
- README "Translating RigTune": where the lang files live, key naming, `%s` arguments, formatting codes, how to test a translation in game, what isn't translatable yet (text from the rules files: recommendation reasons and advice, written in English and fetched remotely; mod names), and how to submit (a PR adding `<locale>.json`).
- A core `Text` value (translation key, arguments, English fallback) for display text that core code builds (Recommender's own titles and reasons such as "Install <mod>", "Version <v> is available (you have <v>)"; UndoPlanner's descriptions and reasons; the queued-update and conflict notes; DependencyResolver/DownloadPlanner/benchmark failure messages shown in the UI). The client renders it with the translation when the key exists, else the fallback; the share report keeps English. Rule-provided titles/reasons stay literal data. New v0.3 code uses `Text` from the start (contracts commit); converting the existing sites is the Wave B l10n workstream.
- `LangCheckTest` (JUnit source scan, no bytecode): (a) no `Component.literal("…")` / `literal(` with a string literal containing letters in client code, except an explicit allowlist (separators, numbers); (b) every translation key referenced literally in client code or `Text` constructions exists in en_us.json; (c) every en_us.json key is used, literally or through a documented dynamic prefix whose suffixes are enumerated from the enum (goal., category., impact., limit., benchmark.scene.); (d) every other `<locale>.json` present has a subset of en_us's keys with the same `%s`/`%n$s` argument count. Dead keys found in research (`rigtune.screen.benchmark`, `.needs_world`) are removed.
- No additional full language in v0.3: quality can't be verified by a native speaker here, and rule text is English-only, so a translated UI would still be half English. Recorded as a v0.4 candidate.
- AC9.1 LangCheckTest passes and fails on a planted literal and a planted missing key (self-test). AC9.2 README section exists. AC9.3 A pseudo-locale check: the test renders every `Text` through a stub translator that uppercases, and no core-built UI string reaches the UI untranslated (unit level). AC9.4 Game test screenshots unchanged in English.

## 10. Report a problem (P1)
Sources: docs/research/v0.3/misc.md §A.
- A **Report a problem** button on the RigTune screen opens `https://github.com/chaotix345/rigtune/issues/new?template=problem.yml&title=<…>&report=<…>` through `ConfirmLinkScreen.confirmLinkNow(screen, URI)` (same signature on 26.2 and 26.3; always shows vanilla's confirm-before-open screen). RigTune sends nothing itself: no HTTP request, no telemetry; the user reads, edits and submits the issue on GitHub.
- `.github/ISSUE_TEMPLATE/problem.yml` (an issue form): "What happened?" (required), "What did you expect?", and `report` (textarea, prefilled). Labels come from the template, never the URL (a bad `labels=` 404s the link).
- The URL is built by pure core code (`core/report/IssueLink`): title `[RigTune <ver>] MC <mc>: `, report = `ShareReport` text; the whole URL is at most 2000 characters; when the report doesn't fit it's cut at a line boundary with a note "(shortened; the full report is on your clipboard)" and the full report is copied to the clipboard at the same time.
- AC10.1 Unit tests: encoding (spaces, `&`, `#`, non-ASCII, newlines); a worst-case report (50 recommendations, long names) gives a URL ≤ 2000 chars that parses as a URI; the field names match problem.yml (the test reads the YAML); no network client is touched. AC10.2 A game test presses the button and screenshots the vanilla confirm screen at 3 sizes, then cancels (nothing is opened).

## 11. Quilt (P2)
Sources: docs/research/v0.3/misc.md §B.
- Quilt Loader lists 26.2/26.3, but QSL/QFAPI were retired at MC 26.1, and whether Quilt Loader loads the real Fabric API on 26.x is unconfirmed. v0.3 doesn't declare Quilt support. README FAQ says so and why.
- AC11.1 README FAQ entry. (No code.)

## 12. spark profiler hint (P2)
Sources: docs/research/v0.3/misc.md §C.
- An info advice rule shown when spark (mod id `spark`) is loaded: how to capture a client profile (`/sparkc profiler start`, play the laggy part, `/sparkc profiler stop`; `--timeout 60`), the privacy note (stop uploads the profile to spark.lucko.me and prints a link), and the reading tips (width = time; the render thread; chunk building; GC and heap; `--thread *`). In v2 and v1 (advice only).
- AC12.1 Scenario test: spark loaded → the advice is shown; not loaded → not. Rules regenerated with item 7.

## 13. Dry-run preview (P2)
- A **Preview** button next to Apply lists exactly what Apply would do for the ticked items, writing nothing: settings written now (file and key, old → new), config files patched at the next restart (Sodium/DH/Iris file and keys), mod files downloaded into `mods/` (file name when known) or renamed to `.disabled` at the next restart.
- AC13.1 A differential unit test: for fixture selections, the preview's file list equals the files the staging then touches in a temp instance. AC13.2 Game test screenshot.

---

## Amendments from the plan review
Source: docs/v0.3/plan-review.md (4 high, 25 medium, 11 low). These override the body where they conflict.

### WS-0 (items 1A/1B, 2)
- W0-M1: Sodium is conditional in the production game-test mods too; game-test assertions that need Sodium skip (and log it) when Sodium isn't loaded, so a node without a Sodium build still gets green legs. The matrix script's unit test covers a node without `sodium_version`.
- W0-M2: no automatic retries (PLAN corrected).
- W0-M3 (revised after WS-0's code review): the in-CI Modrinth publish uploads exactly the files uploaded to the GitHub release, so the first run is byte-identical by construction. A job re-run does NOT recover a partial failure (the release step fails on the existing release; an idempotent step would make already-published nodes fail the loop). The recovery runbook (release.yml comment + docs/v0.3/design/ws-0.md): download the failed node's asset from the GitHub release and upload it with `tools/modrinth_project.py upload-version`; never rebuild. A guard fails the release when the tag doesn't equal `mod_version`. AC1.5 adds a reproducibility note (two clean builds compared by sha256).
- W0-L1: build.yml gets a per-ref `concurrency` group with `cancel-in-progress: true`.
- H-M1/H-M3: CI compiles the e2e drivers (`compileE2eUndoJava`; `compileE2eJava` against the released 0.1.0 and 0.2.0 jars, downloaded from the GitHub releases, sha256 checked). If the 0.2.0 compile fails, WS-0 adds the parameterisation or reports it for WS-H.

### WS-A (3a-3c)
- A-H1 (replaces 3b's design): `DownloadPlanner.plan` handles every UpdateMod before any AddMod. `OnlineDataFetcher.Result` also keeps each update's full `ModrinthVersion` (with dependencies) and a versionId → projectId map (`UpdateInfo` unchanged: the e2e driver reads it). Each staged update enters the batch's version set, and its own `incompatible` dependencies are checked against installed projects and the batch, both directions. An addition refused against the pre-update view but passing against the post-update view is allowed only by **joining that update's group** (both apply or neither; 3a's drop removes both). AC3.2 becomes: (i) B incompatible with A v1, A updated to v2 in the batch → B joins A's update group; (ii) B being added, incompatible with the A v2 being updated to → refused; (iii) an update whose own version declares `incompatible` on an installed project → refused; (iv) selection order doesn't change the outcome; (v) a failed update download refuses the dependent addition. Known gap if not covered: an installed mod's own `incompatible` entry against an update target.
- A-M1 (replaces 3a's bookkeeping): `Staging.stage` returns the merge result; RealController keeps recommendation id → surviving op ids; after any drop, undo or discard, a recommendation id stays staged only while one of its op ids is still in pending.json. AC3.1's last clause becomes: for every id in `staged`, at least one recorded op id is in pending.json, and none of a dropped group's recommendation ids remain.
- A-M2: 3c names the project from local data only (installed: the versionId → projectId map; batch: `ModrinthVersion.projectId()`), then title, slug, "another mod". **No new Modrinth call or endpoint.**
- A-L1: 3a's notice says "change"; WS-A owns OnlineLookupGate for the raw game version and adds a named seam for its test; the offline availability map keyed by the normalized version misses only on snapshots (recorded).

### WS-B (6, 3e)
- B-H1: a "superseded" pass inside `UndoPlanner.build()` (so `recheck()` applies it too; a no-op for Undo last / Undo everything): a selected change is SKIP "Changed again by a later apply" when any later non-undo entry has a change that is STAGED, or APPLIED and not reverted, on the same settings key, the same `file`/`resultFile` name (either direction), or an enable with the same `modId`; its group is skipped with it. AC6.1 adds: a same-name update chain (Apply 1 v1→v2, Apply 2 v2→v3, Undo this on Apply 1 → SKIP), a staged config patch after an applied one, and a later entry that was itself undone (the older one can then be undone).
- B-M1: 3e's fixture is a templated read-only copy of the user's real 0.1.0 `last-apply.json` (it holds the failed DH group) plus the hand-written FAILED fixture; WARN lines are logged once per `finishedAt` (remembered in ClientState next to `lastShownApply`); "attempt n of 3" uses n = attempts + 1; ABANDONED changes show their reason too ("Not applied: …").
- B-M2: WS-B may add one read-only, helper-safe accessor `Journal.state()` (MISSING/OK/CORRUPT/NEWER); HelperLauncherTest must still pass. The corrupt message says a backup will be kept when RigTune next writes the history.
- B-M3: the e2eUndo driver gets a per-entry case (two applies each adding a mod; Undo this on the older one; restart; only the older mod is disabled; both journal entries right) → AC4.2 (WS-H implements; WS-B provides the API it drives). WS-B keeps the `UndoScreen(Screen, RigTuneController, boolean)` constructor.
- B-L1: "fully undone" = no change left that can be undone; Undo this is offered for apply, benchmark and legacy-import entries (as Undo last); a disable + enable in one group with the same mod id shows as "Updated <mod>"; AC6.4 moves to WS-G's verification.
- X-M2 (layout; WS-B owns the footer): the main screen's "Undo last" and "Undo all" move into the History screen (next to per-entry Undo this); the main footer gets "History…" in their place. WS-F adds "Report a problem" right after "Copy report". AC6.3 and AC10.2 add a 640×480 GUI-scale-2 screenshot.

### WS-C (5)
- C-M1: detection reads the game dir and its parent only (absolute, normalized); a file is opened only if `Files.isRegularFile` (a symlink to a regular file is fine) and its size is checked first; `instance.cfg` ≤ 64 KiB; `minecraftinstance.json` through Gson's streaming `JsonReader`, reading only the top-level `isMemoryOverride` and stopping after 32 MiB; the probe runs on `Probes.EXECUTOR` with a timeout; any Throwable → Unknown. AC5.1 adds directory-named-`instance.cfg`, symlink and large-file fixtures (and a FIFO where the OS supports it).
- C-M2: Modrinth App and Prism always get the **instance-level** steps (turn on the instance's memory override, then set the maximum); CurseForge uses `isMemoryOverride` to pick per-pack vs global steps. RigTune doesn't read `MaxMemAlloc`/`allocatedMemory` (`Runtime.maxMemory()` is the effective value).
- C-M3: the official launcher's brand literal is verified from public crash reports/logs of official-launcher users if at all possible; if not verifiable, the official launcher stays Unknown and PROGRESS records it as a P1 deviation with the reason.
- C-L1: LauncherGameTest (not UiGameTest); confirm the "without brand" screenshot against the CI JVM arguments/latest.log; AC5.2's scenario tests live in WS-C's own test class; ShareReport gets an overload (no signature change).

### WS-D (7, 12, vulkan-backend, C, 3d)
- D-H1: **every new tier row carries `"v1": false`**: 0.1.x keeps its v0.1.0 classification for all new hardware (a lower tier is not "more conservative" by RulesV1DifferentialTest's definition). AC7.2: each new matrix entry pairs the GPU with a tier-5 CPU (a K or X3D model) and a heap ≥ 6 GB so the GPU row decides the tier, and the test shows no difference against the baseline; plus a unit test that the pinned v0.1.0 classifier gives the new strings the same tier and integrated flag under the old and new rules-v1.json.
- D-M1: weekly update-rules PRs target main at revision 10 while WS-D's run on feat/v0.3.0 also makes revision 11. Disabling the cron was refused by the permission classifier, so: any bot PR is triaged by re-running the updater on feat/v0.3.0 instead of merging it to main, and in Phase 7, after merging `origin/main` into the release branch, the updater is re-run so the release's revision is strictly higher than main's. AC4.5 adds: the release commit's rules revision > main's revision before the merge.
- D-M2: AC1.7: KnowledgeV2ScenarioTest covers `vulkan-backend` on 26.2 and 26.3 (shown on Vulkan), `26.4-alpha.1` and `26.4` (not shown); rules-v1.json keeps its current `when` for this advice (unit test or check_rules_v1.py).
- D-L1: `v1` on tier rows is a source-only key (stripped from both outputs; validate_knowledge accepts it; SchemaConsistencyTest stays consistent). The spark advice is `"v1": false`, `impact: low`, short, and says that `/sparkc profiler stop` uploads the profile (with system and JVM details) to spark.lucko.me behind a shareable link (verify the contents).

### WS-E (3f, 8)
- E-M1: the settle area is a circle (dx² + dz² ≤ (RD−1)², in chunks, from the camera's chunk); a step whose settle timed out with more than 2% of in-range chunks missing counts as **not passing** for the up-search and logs its missing count; a core `SettleCheck` with unit tests.
- E-M2: in the CURRENT scene, Tune's up-steps are capped at the join-time render distance + 8, and the menu shows a one-line note before a CURRENT-scene Tune ("Testing higher render distances loads and saves more of this world"); DESIGN and README say so.
- E-M3: the settle timeout is configurable; the game test uses 60 s, asserts the settle didn't time out, then that the chunks are present, and logs the settle time per leg.
- E-L1: the shader advice wording stays generic unless the menu path is verified in the Phase 5 smoke; a dev-only `-Drigtune.dev.targetFps` forces the advice for AC8.7's smoke; the camera-height change asserts the camera block and the one above are air; `broadcastOptions()` must be a no-op without a player/connection; simulation distance isn't part of the client information, so no claim is made about SD broadcasting.

### WS-F (10, 11)
- F-M1: spike first (screenshot `confirmLinkNow` with a 2,000-character URL at 640×480 scale 2). Default design: the URL carries the title and a short `report` (versions + hardware lines, about 500 characters); the **full** report is always copied to the clipboard (a status line says so) and problem.yml asks the reporter to paste it. AC10.1: URL ≤ 800 characters.
- F-L1: encode with a form encoder (`+` → `%2B`; RigTune versions contain `+`), AC10.1 adds `+` and `%` cases; problem.yml reaches main with the release PR (before the tag); WS-F may add one RealController/RigTuneController method for the versions.

### WS-G (9)
- G-H1: "from the start" is dropped: no `Text` contract in Wave A. Wave A writes UI text as lang keys in client code (as today); WS-G converts core-built text in Wave B.
- G-M1: `Text` is added **next to** existing String accessors (e.g. `titleText()`); no existing signature changes (the e2e drivers call `title()`, `reason()`, `UndoScreen(...)`); no persisted string (pending.json, last-apply.json, history.json, benchmarks.json) and no class reachable from ApplyHelper changes; HelperLauncherTest and the e2e driver compiles are part of WS-G's checks.
- G-M2: AC9.3 becomes: for fixture Reports, UndoPlans and resolver/planner errors, every field the UI shows is a `Text` whose key is in en_us.json, and rendered through an uppercasing stub translator the output is uppercase except argument values. LangCheckTest (a) also flags string pieces with 2+ letters inside `text(`, `centeredText(` and `literal(` calls and in `+` chains in client/ui, with an allowlist; UndoPlanner's description texts are in scope.

### WS-H (4)
- H-M2: AC4.1 adds a seeded 0.1.0 → 0.3.0 variant built from templated read-only copies of the user's real `pending.json` and `last-apply.json` (the failed DH group, attempts 1/3) and minimal fake jars with fabric id `distanthorizons` (3.3.0 in mods/, 3.3.2 in mods/update/<folder>/). Pass: after the first 0.3.0 launch the carried-over DH group is dropped with the notice, the journal marks it DISCARDED, latest.log has the 3e WARN line, and nothing in mods/ changes at exit.

### WS-V (1)
- V-M1: add_mc_version.py stays in scope (the brief asks for a one-step procedure) but is first to cut among item 1's parts; mc_apidiff.py is P1. Changes A, B, C, D (the committed harness), the raw game version and the vulkan rule stay P0.
- V-L1: WS-V documents its tools in a new tools/MC_VERSIONS.md (tools/README.md gets one link line, added by the coordinator) and owns tools/tests/test_add_mc_version.py.

### Cross-cutting
- X-M1: Wave A's final local game-test runs are optional where the CI legs cover the change; the lock is reserved for WS-E's autorun (AC3.7), WS-H's E2E runs and Phase 5. If the lock is busy, don't wait: CI evidence suffices for Wave A.
- X-L1/X-L2/X-L3: hotspot table extended (PLAN); docs/modrinth/body-0.3.md, CHANGELOG and version lines go to a Phase 7 docs task; AC3.4 is re-checked by the coordinator before the tag; AC9.4's "unchanged" is a pixel diff of CI screenshot artifacts before/after WS-G; WS-P runs after WS-G merges, or is cut.
- Cut order if time runs short: P2 preview → the core `Text` conversion (keep LangCheckTest, the guide, dead keys) → mc_apidiff.py, then add_mc_version.py → spark advice, Quilt FAQ → the shader advice line and camera height → launcher file walking (keep property/env detection) → per-entry Undo this (keep the read-only History screen) → Report a problem's prefilled report (keep clipboard + blank form with title). Never cut: item 2, 3a-3f, item 4 (both E2E paths, the seeded variant, byte-identical release), item 7 with D-H1.
