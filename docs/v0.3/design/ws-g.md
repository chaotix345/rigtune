# WS-G: localisation (SPEC item 9)

Branch `feat/l10n`. Plan: docs/v0.3/plans/ws-g.md. Amendments applied: G-H1 (no Wave A contract; converted here), G-M1 (Text next to the Strings, no signature change, nothing persisted or on the helper path), G-M2 (AC9.3 as fixture checks; LangCheckTest (a) widened), B-L1 (AC6.4 verified here), X-L2 (AC9.4 as a CI screenshot pixel diff).

## Text
- `core/model/Text` (sealed, no Minecraft imports): `Translatable(key, fallback, args)` (the English template and its arguments: String, Number, Boolean or a nested Text), `Literal(value)` (data, never formatted or translated) and `Joined(separator, parts)` (sentences of one paragraph, `"; "` lists; `join()`/`sentences()` leave out blank parts, the constructor keeps them; the English is the parts with the separator, never trimmed, so the client shows exactly `english()`).
- `english()` formats the fallback exactly as Minecraft's `TranslatableContents.decomposeTemplate` does. Checked with `javap -c` on 26.2's and 26.3's `minecraft-common.jar` (the bytecode is identical): pattern `%(?:(\d+)\$)?([A-Za-z%]|$)`, `%%` is `%`, `%s` takes the next unnumbered argument (a counter `%n$s` doesn't move), any other conversion, a stray `%`, a missing argument or an argument number too big for an int makes the whole template show raw; a null argument reads "null". `TextsTest` renders the same Texts through real `Component`s and gets the same strings.
- `client/ui/Texts.component(Text)`: `Component.translatableWithFallback(key, fallback, args)` (exists on 26.2 and 26.3, `javap`), so the language's template is used when it has the key and the English otherwise, resolved at render time like every other translatable.
- `TextException extends IOException` carries a Text for refusals the UI shows; its message is the English.

## What changed where (every String keeps its old English, G-M1)
| type | added | old API kept |
|---|---|---|
| `Recommendation` | components `titleText`, `reasonText`; `of(id, category, impact, Text, Text, action, selected)` sets `title`/`reason` to their English | 7-arg constructor (texts become literals of the Strings), `title()`, `reason()` |
| `UndoPlan.Item` | components `descriptionText`, `reasonText` (null when `reason` is); `Item.of(...)` | 6- and 4-arg constructors, `description()`, `reason()` |
| `DownloadPlanner.Result` | component `errorTexts` | 3- and 4-arg constructors, `errors()` |
| `ApplyPreview.Download` / `Disable` / `Skipped` (WS-P) | `titleText`; `Skipped.detailText` | their old constructors, `title()`, `detail()` |

`javap` on the built 0.3.0 jar lists every old constructor and accessor with its old descriptor, and `compileE2eUndoJava` plus `:26.2:compileE2eJava` against the released 0.1.0 and 0.2.0 jars (sha256 as in build.yml) compile.

Converted (keys, English unchanged):
- Recommender (`rigtune.rec.*`): Disable/Install/Update titles, the obsolete and "doesn't suit this hardware" reasons, the conflict title and reason, "RigTune doesn't also offer X (or Y), which conflict(s) with it", the alpha and availability notes, the update reason, the outside-the-mods-folder and bundled notes, queued update, "updates itself", "Update RigTune", and the setting title (`%s: %s → %s`, the label and values are arguments). ModrinthOffAdvice's four notes (`rigtune.rec.modrinth_off.*`, `rigtune.rec.network_off.*`). Rule-provided titles, reasons and advice text stay `Literal`. The joins reproduce the old String code exactly: an addition's rule reason is kept as it is without notes and joined like the old `(reason + " " + note).trim()` with them; a disable outside the mods folder is `reason + " " + note` even for a blank reason; ModrinthOffAdvice drops a blank reason (RecommenderTextTest checks spaces at the edges and empty rule text).
- UndoPlanner (`rigtune.undo.item.*`, `rigtune.undo.reason.*`): every description (setting, enable, disable, re-enable, patch, unknown change, "(none)") and every reason constant (the constants stay: the game tests compare with them); "would stop the game from starting" lists its causes (`breaks.twice`, `breaks.missing`) as a `"; "`-joined Text, the causes sorted by their English as before.
- DependencyResolver and DownloadPlanner (`rigtune.download.*`): every refusal of their own is a `TextException`; `errorTexts` entries are `rigtune.download.error` ("%s: %s", the recommendation's title Text and the cause). "another mod" is a key.
- Preview (WS-P, merged in): its rows show the recommendation's `titleText`; a refused download's reason is the planner's `TextException` text taken from `errorTexts`; "Value contains control characters" is `rigtune.preview.detail.control_characters`. The game's and Sodium/DH/Iris config patchers' own refusal messages stay `Literal`.
- Client: RigTuneScreen shows titles/reasons through `Texts` (a vanilla setting's caption title through `rigtune.rec.setting.title`), the header's GB values and display size through `rigtune.unit.gb`, `rigtune.header.display_size(_hz)`; UndoScreen shows the item Texts; RealController joins `errorTexts` for "Download failed: %s".

Not changed on purpose:
- Persisted text and the helper: `last-apply.json` reasons (the History screen shows the helper's English reason as an argument), `history.json`, `benchmarks.json` (a benchmark check's `"failed: <message>"` keeps its format; the prefix is now the constant `SessionResult.NOT_MEASURED_FAILED`), `pending.json`. No class under core/apply, nor Journal/JournalChange/JournalEntry, changed; HelperLauncherTest passes.
- The share report (ShareReport reads the English Strings), the logs, DevAutorun's `PlannerResult.reason()` (log only).
- Error details from outside RigTune's planning stay `Literal`: network/HTTP, redirect and SHA-512 errors from the Modrinth client (HttpModrinthClient, not in this item's list), SafeFileNames' unsafe-name error (core/apply: helper path), SettingsBridge's and the config patchers' refusal messages, a benchmark check's exception message.
- Sorting: recommendations are still ordered by their English title (the comparator reads `title()`), so a translated list keeps the English order.

## LangCheckTest (AC9.1, AC6.4)
`src/test/java/io/github/chaotix345/rigtune/LangCheckTest.java`, a source scan over `src/client/java` and `src/main/java` with a small Java lexer (comments and string contents masked, so parentheses and `+` inside strings or comments don't count):
- (a) client code: a string literal with a letter inside `literal(...)` anywhere in `src/client/java`; in `client/ui` any string literal with two or more letters (inside `text(...)` / `centeredText(...)`, next to `+` / `+=`, in a constant, ...; widened after the code review). Exempt: keys, key prefixes and suffixes (`rigtune.goal.`, `.tooltip`), dotted identifiers (`vanilla.renderDistance`), and the arguments of `LOGGER.*`, `replaceAll`/`replaceFirst`/`matches`/`split`/`compile`, `ofPattern` and `Thread` calls. `ALLOWED` (file + literal + reason): RigTuneScreen's `OpenGL`/`Vulkan` (API names) and `true`/`false` (option values compared, shown as the game's On/Off). On the Wave A tree the scan found the header's `" Hz"` / `" GB"` (now keys) and BenchmarkResultScreen's `"failed: "` protocol prefix (now a constant); after merging WS-P, PreviewScreen's thread name (now exempt as a `Thread` argument).
- (b) every key-shaped `rigtune.*` / `key.rigtune.*` literal in client and core (Component.translatable, Text.of, LauncherInfo's name/steps tables, HistoryModel, UndoPlan problems, BenchmarkController refusals) is in en_us.json; `NOT_KEYS`: `rigtune.json` (file), `rigtune.dev.autorun` (system property).
- (c) every en_us.json key is written in the code or belongs to a family whose values come from the code: `rigtune.goal.<Goal>` (+ `.tooltip`), `rigtune.category.<Category>`, `rigtune.impact.<Impact>`, `rigtune.limit.<every limitingFactor TierCalculator returns over all tiers and goals>`, `rigtune.benchmark.scene.<Scene>`, `rigtune.benchmark.menu.scene.<Scene>.hint`, `rigtune.history.kind/status.*` (HistoryModel.kindKey/statusKey over the journal constants), BenchmarkController's `rigtune.benchmark.{cancelled,throttled}` + `.title`/`.body`, and `key.category.rigtune.rigtune` (Minecraft's name for the key-mapping category RigTuneClient registers; the test checks the registration line). Each family's prefix and suffix literals must appear in the code and each family key must be in en_us.json.
- (e) every `Text.of(...)` in client and core writes its key out, and its English, worked out from string literals and the same file's `static final String` constants joined with `+` (UndoPlanner's reason constants, Recommender's notes, ModrinthOffAdvice's notes), is en_us.json's value for that key (added after the code review: a constant that drifts from en_us.json would make the English screen differ from the Strings).
- (d) every other locale file (a Minecraft language code: `[a-z]{2,4}(_[a-z0-9]{2,4})?.json`): a flat object of strings, keys a subset of en_us.json's, the same argument set (`%s` counted in order, `%n$s` by position) and a template Minecraft can format. A lang file's `%d`/`%.1f` count as `%s`, as `Language.loadFromJson` rewrites them (`%(\d+\$)?[\d.]*[df]` → `%$1s`, javap on 26.2 and 26.3); en_us.json may use only `%s`, since the code's English fallback isn't rewritten.
- Self-tests plant a literal (in `literal`, `text`, `centeredText`, `+`, `+=`, a constant, and ones that must not count: in a comment, a log call, a regex, a thread name, a setting key), a missing key, an unused key, a family with a missing key and an unwritten prefix, `Text.of` with a wrong constant English, an unwritten key and an English that can't be worked out, and locale files with an extra key, a wrong argument set, a stray `%`, a non-string value and a bad file name. Check (a) run once by hand over the pre-WS-G client sources (b0a6da6, a throwaway main in the scratchpad) reports exactly the three hits above.
- Dead keys removed: `rigtune.screen.benchmark`, `rigtune.screen.benchmark.needs_world` (the only ones (c) found).

## AC9.3 (pseudo-locale)
`TextChecks` (test helper): every `Translatable` key is in en_us.json and its fallback is the en_us.json value; a `Literal` outside an argument must be blank or rule data; rendered with a translator that uppercases every en_us.json template (keeping `%s`/`%n$s`/`%%`) and with the arguments and rule data replaced by a placeholder, nothing is lower case. (Arguments are data by definition, as AC9.3 says: English passed as an argument String wouldn't be caught; LangCheckTest's (e) and the key-per-branch assertions cover what the core writes.)
- Reports: `PseudoLocaleTest` over `L10nFixtures` (rules reaching every Recommender branch with text of its own, plus ModrinthOffAdvice with Modrinth off and with the network off), and the same through a Minecraft `Language` injected with the pseudo-locale (real Components). Example: `Faster net. RIGTUNE DOESN'T ALSO OFFER Noise OR Surface, WHICH CONFLICT WITH IT. (AVAILABILITY NOT CONFIRMED)`.
- Undo plans: an `@AfterEach` in UndoPlannerTest checks every plan its 70 tests make (description and reason Texts, their English equal to the Strings); `UndoPlannerTextTest` maps each reason constant to exactly one `rigtune.undo.reason.*` key.
- Planner/resolver errors: DependencyResolverTest's refusals go through a `refused(...)` helper that requires a `TextException` passing the check; an `@AfterEach` in DownloadPlannerTest checks every error (a literal cause is allowed only for the fake's download failure); `DownloadErrorTextTest` covers the refusals the other tests don't reach (no version, no file for an add and an update, outside the mods folder) and a download's own failure.
- The check has its own planted-problem test (a core-built literal, a missing key, a wrong English, a literal sentence joined to a key).

## Phase 5 lows (coordinator, while in the text code)
- The History screen's failed change read "Last attempt failed: Gave up after 10 attempt(s): … (attempt 2 of 3)": two different counts. It now ends "(try 2 of 3 at restart)"; the startup WARN line says "restart attempt n of 3" (the E2E harness's `attempt \d+ of 3` regex still matches). ApplyFailuresTest, HistoryScreenTest and HistoryGameTest updated.
- The apply toast said "Mod files and Sodium settings were updated." for any staged change (DH and Iris config patches too): now "RigTune's staged changes were applied."

## Code review
Code-reviewer subagent on `git diff origin/feat/v0.3.0...HEAD` (before the WS-P merge): no high, 2 medium, 9 low, 2 nits. Fixed: (1, medium) (a) widened to every client/ui literal; (2, medium) check (e) for constant English; (3, 4) exact English for whitespace-edged or empty rule text, and the client equal to `english()`; (5) huge `%n$s`; (6) language-code file names; (7) `%d`/`%f` in lang files; (8) placeholder masking in TextChecks; (9) `Result(…, null)`; (10, 11) README accuracy; (12) the download keys' place. Not changed: (13) Undo's re-check shows a vanished change's id as its description, as before (data).

## AC9.4 / AC6.4 (screenshots unchanged in English)
Method (X-L2): `gh run download <run> -p 'gametest-screenshots-*'` for both runs, a Pillow `ImageChops.difference` per frame, the differing pixel rows merged into bands, and each differing frame put in a category whose band it must stay inside (e.g. a main-screen frame may differ only in the header's first line). Full per-frame output: docs/v0.3/verification/ws-g/screenshot-diff.txt.
- Final WS-G run 36192741980 (74ee421) against 36191509381 (feat/v0.3.0 @ b84549d: the same tree without WS-G): 261 frames each, 100 identical, 161 differing, every one explained: 75 main-screen frames differ only in the header's CPU line (the CI runner's CPU: AMD EPYC 7763 vs 9V74, and Xeon models on other runs), 6 more also in "Copied the report (N characters)" (N follows the CPU name), 9 Report-a-problem link screens (the link carries the CPU), 9 Undo screens only in the subtitle's date and time, 6 History lists in their entry times, 6 title screens (panorama, splash text), 14 world renders, 24 benchmark screens (FPS values, charts, dates), and 12 History details frames in the failed change's line, which now ends "(try 2 of 3 at restart)" (the deliberate Phase 5 wording above). So the recommendation list at every size (including the Modrinth-off and network-off notes), the Undo item lists, the launcher lines, the Preview screens (canned and real) and the History rows are pixel-identical in English.
- Against 36182135247 (81067aa, the baseline named in the brief, before rules r12 and WS-P): 70 identical, 191 differing; the extra ones are the header's rules revision (r11 → r12), the ram-* advice text of rules r12 (fix/ram-advice-text), WS-P's Preview button in the footer (and the list end it moves) and 24 new Preview frames; comparison A above isolates WS-G from those.
- For scale: two runs of the same feat/v0.3.0 code (36182135247 vs 36184044794) already differ in 146 frames.

## Notes for other workstreams
- Sorting, the share report and the logs stay English by design (see above).
- New display text built in core should be a `Text.of("rigtune.<area>.<thing>", "English %s", args)` with the key added to en_us.json; LangCheckTest checks the English matches.
